// Initialization for ES Users
import {
    Dropdown,
    initTWE,
  } from "tw-elements";
  
  initTWE({ Dropdown });
  // Initialization for ES Users
import {
    Collapse,
    Dropdown,
    initTWE,
  } from "tw-elements";
  
  initTWE({ Collapse, Dropdown });
  // Initialization for ES Users
import {
    Ripple,
    initTWE,
  } from "tw-elements";
  
  initTWE({ Ripple });